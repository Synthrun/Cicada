{
  "$schema": "https://opencode.ai/schema/opencode.json",
  "name": "cicada",
  "description": "Security audit & fix skill for web and mobile apps",
  "version": "1.0.0",
  "agents": {
    "cicada": {
      "description": "Run a full security audit — backend + mobile — with optional auto-fix",
      "skills": ["SKILL.md"],
      "command": "/cicada",
      "triggers": [
        "security audit",
        "vulnerability scan",
        "check for vulnerabilities",
        "audit my app",
        "cicada audit",
        "/cicada"
      ]
    }
  }
}
